#include <stdio.h>
/**
 * main - A program that prints _putchar, followed
 * by a new line.
 *
 * Return: Exit program.
 */

int main(void)
{
	char output[] = "_putchar";
		for (int i = 0; i <= 7; i++)
		{
			putchar (output[i]);
		}
	return (0);
}
