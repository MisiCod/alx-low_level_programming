#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string.
 * @s: passed character
 *
 * Return: program exit.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
